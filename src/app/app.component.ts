import { Component } from '@angular/core';

@Component({
	selector: 'app-root',
	template: `
		<!-- Initial router outlet for children components -->
		<router-outlet></router-outlet>
	`
})
export class AppComponent {
	
}
